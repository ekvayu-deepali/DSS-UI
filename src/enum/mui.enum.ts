export enum AnchorOriginHorizontalEnum {
  CENTER = 'center',
  LEFT = 'left',
  RIGHT = 'right',
}

export enum AnchorOriginVerticalEnum {
  BOTTOM = 'bottom',
  CENTER = 'center',
  TOP = 'top',
}

export enum TransformHorizontalOrigin {
  CENTER = 'center',
  LEFT = 'left',
  RIGHT = 'right',
}

export enum TransformVerticalOrigin {
  BOTTOM = 'bottom',
  CENTER = 'center',
  TOP = 'top',
}
